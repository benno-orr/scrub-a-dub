% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepost_umap.R
\name{sad_prepost_umap}
\alias{sad_prepost_umap}
\title{Pre- and post-filtering UMAP, side by side}
\usage{
sad_prepost_umap(
  raw_pcs,
  filtered_pcs,
  metadata,
  dataset = "dataset",
  keep_col = "keep",
  doublet_col = "doublet",
  n_pcs = 20L,
  seed = 1L
)
}
\arguments{
\item{raw_pcs}{list(barcode, mat) from [sad_read_pcs()], or a data.frame
with a `barcode` column plus PC columns, covering every raw cell}

\item{filtered_pcs}{same shape as `raw_pcs`, but only the retained cells,
refit however you refit it (fresh PCA on the subset is typical)}

\item{metadata}{data.frame with a `barcode` column plus `keep` and
`doublet` logical columns, covering (at least) every cell in `raw_pcs`}

\item{dataset}{label used in panel titles}

\item{keep_col}{name of the logical "passed non-doublet QC" column}

\item{doublet_col}{name of the logical "flagged doublet" column}

\item{n_pcs}{number of leading PCs to embed on}

\item{seed}{random seed, reused (and +1'd for the post panel) for
reproducibility}
}
\value{
a [patchwork] object (pre panel | post panel); `print()` or
  [ggplot2::ggsave()] it
}
\description{
Embeds `raw` (every cell that entered the QC step) and `filtered` (the
cells you actually kept) independently, and returns both panels as one
[patchwork] figure. The left panel colors every raw cell by why it was
later dropped so you can see where doublets/low-quality cells sit
relative to the clusters they blur; the right panel is the resulting
embedding once they're gone.
}
\details{
scrubadub does not call doublets or decide what "low-quality" means --
`metadata` must already carry those calls (e.g. from your own doublet
classifier, `duddrop`, or a manual cutoff).
}
